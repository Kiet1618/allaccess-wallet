export type Theme = {
    mode: string;
    text: string;
    color: {
        backgroundPrimary: string,
        backgroundSecondary: string,
        primary: string,
        secondary: string,
    }
}
