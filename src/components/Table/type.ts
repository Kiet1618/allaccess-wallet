export type InfoModal = {
  time: string;
  method: string;
  amount: string;
  from: string;
  to: string;
  network: string;
  id: string;
  token: string;
};
export type PropsInOut = {
  text: string;
};
